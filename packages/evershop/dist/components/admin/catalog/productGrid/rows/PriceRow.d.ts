declare function ProductPriceRow({ areaProps: { row } }: {
    areaProps: {
        row: any;
    };
}): React.JSX.Element;
declare namespace ProductPriceRow {
    namespace propTypes {
        let areaProps: any;
    }
}
export default ProductPriceRow;
import React from 'react';
