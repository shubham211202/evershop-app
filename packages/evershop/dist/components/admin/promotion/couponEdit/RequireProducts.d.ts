export function RequiredProducts({ requiredProducts }: {
    requiredProducts: any;
}): React.JSX.Element;
export namespace RequiredProducts {
    namespace propTypes {
        let requiredProducts: any;
    }
    namespace defaultProps {
        let requiredProducts_1: never[];
        export { requiredProducts_1 as requiredProducts };
    }
}
import React from 'react';
