declare function ActionColumnRow({ areaProps: { row } }: {
    areaProps: {
        row: any;
    };
}): React.JSX.Element;
declare namespace ActionColumnRow {
    namespace propTypes {
        let areaProps: any;
    }
}
export default ActionColumnRow;
import React from 'react';
